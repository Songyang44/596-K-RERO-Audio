import type { DatasetChanges } from "@ldo/subscribable-dataset";
import type { QuadMatch } from "@ldo/jsonld-dataset-proxy";
import type { Quad } from "@rdfjs/types";
export type TripleMatch = [QuadMatch[0], QuadMatch[1], QuadMatch[2]];
export declare class UpdateManager {
    private tripleMatchListenerMap;
    private listenerHashMap;
    private tripleMatchToHash;
    registerListener(tripleMatch: TripleMatch, callback: () => void): void;
    removeListener(callback: () => void): void;
    notifyListenersOfChanges(changes: DatasetChanges<Quad>): void;
}
