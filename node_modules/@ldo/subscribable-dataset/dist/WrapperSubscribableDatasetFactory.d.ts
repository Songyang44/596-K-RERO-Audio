import type { DatasetFactory, BaseQuad, Dataset } from "@rdfjs/types";
import WrapperSubscribableDataset from "./WrapperSubscribableDataset";
export default class WrapperSubscribableDatasetFactory<InAndOutQuad extends BaseQuad = BaseQuad> implements DatasetFactory<InAndOutQuad, InAndOutQuad> {
    private datasetFactory;
    constructor(datasetFactory: DatasetFactory<InAndOutQuad, InAndOutQuad>);
    dataset(quads?: Dataset<InAndOutQuad, InAndOutQuad> | InAndOutQuad[]): WrapperSubscribableDataset<InAndOutQuad>;
}
