import type { ContextDefinition } from "jsonld";
export declare class ContextUtil {
    readonly context: ContextDefinition;
    private iriToKeyMap;
    constructor(context: ContextDefinition);
    keyToIri(key: string): string;
    private keyIdToIri;
    iriToKey(iri: string): string;
    getType(key: string): string;
    isArray(key: string): boolean;
    isLangString(key: string): boolean;
}
